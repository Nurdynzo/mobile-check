import {StyleSheet} from 'react-native';
import {wp} from '@/resources/config';

export const listRendererScreenStyles = StyleSheet.create({
  flex1: {flex: 1},
  default: {paddingBottom: 10},
  gap: {height: wp(16), width: '100%'},
});
