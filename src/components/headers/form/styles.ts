import {hp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const formHeaderStyles = StyleSheet.create({
  container: {
    alignContent: 'center',
    gap: 8,
    marginBottom: hp(32),
  },
});
