import {wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const specializedProcedureViewStyles = StyleSheet.create({
  result: {marginBottom: 0, paddingHorizontal: wp(12)},
});
