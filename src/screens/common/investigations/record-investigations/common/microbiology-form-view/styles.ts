import {wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const microbiologyFormViewStyles = StyleSheet.create({
  padding: {
    paddingHorizontal: wp(24),
    paddingBottom: wp(16),
  },
});
