import {hp} from '@/resources/config';
import {StyleSheet} from 'react-native';

const forgotPasswordScreensHeaderStyles = StyleSheet.create({
  container: {paddingTop: hp(80), gap: hp(20), marginBottom: hp(16)},
});

export default forgotPasswordScreensHeaderStyles;
