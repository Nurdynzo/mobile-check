import {hp, wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const loginScreenStyles = StyleSheet.create({
  screen: {
    paddingTop: hp(40),
  },
  searchResultCard: {marginBottom: wp(4)},
});
