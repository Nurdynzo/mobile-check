import {wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const physicalExaminationStyles = StyleSheet.create({
  subHeader: {
    paddingHorizontal: wp(24),
    paddingBottom: wp(16),
  },
});
