import {wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const addVitalSignsButtonStyles = StyleSheet.create({
  sheetFooter: {
    marginHorizontal: wp(20),
    marginBottom: wp(50),
    alignSelf: 'flex-end',
    marginTop: wp(16),
  },
});
