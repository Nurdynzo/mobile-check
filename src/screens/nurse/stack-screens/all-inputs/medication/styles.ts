import {wp} from '@/resources/config';
import {StyleSheet} from 'react-native';

export const medicationStyles = StyleSheet.create({
  tabSwitcherContainer: {paddingHorizontal: wp(24), paddingBottom: wp(16)},
});
